# Label non-single/multiple choice/date(time) fields `singlechoice_factor`, `multichoice_factor`, `rc_date` and `rc_datetime`

Label non-single/multiple choice/date(time) fields
`singlechoice_factor`, `multichoice_factor`, `rc_date` and `rc_datetime`

## Usage

``` r
label_others(data, metadata)
```

## Arguments

- data:

  dataframe

- metadata:

  redcap data dictionary
